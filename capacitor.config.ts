import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Tour_of_Heroes',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
